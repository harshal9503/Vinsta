// Placeholder for utility functions
// Add utility functions as needed for the app

export const formatPhoneNumber = (phone: string) => {
  // Example utility: format phone number
  return phone.replace(/(\d{3})(\d{3})(\d{4})/, '($1) $2-$3');
};
